import React, { Component } from 'react';
import { IProps as P } from "./Types";
import { CarouselContext } from "./CarouselContext";
export interface IProps extends P, React.HTMLAttributes<HTMLDivElement> {
}
export interface IState {
    left: number;
}
export default class SliderBar<P extends IProps, S extends IState> extends Component<P, S> {
    context: React.ContextType<typeof CarouselContext>;
    prevContext: React.ContextType<typeof CarouselContext>;
    constructor(prop: P);
    /**
     * Slide to a specific slide by index
     *
     * @param nextSlide
     */
    slideTo: (nextSlide: number) => void;
    onScroll: (trayWidth: number, slideWidth: number, scrollLeft: number) => void;
    componentDidUpdate(prevProps: Readonly<IProps>, prevState: Readonly<IState>, snapshot?: any): void;
    componentDidMount(): void;
    componentWillUnmount(): void;
}
